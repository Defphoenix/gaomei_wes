name: WES code regressions

on:
  push:
  pull_request:

jobs:
  lightweight-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run shell and parser regressions
        run: bash scripts/run_code_tests.sh
